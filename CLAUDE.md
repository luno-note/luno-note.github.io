# Project notes

This project is a standard Vite + React app.

- Use the normal local workflow: `npm install`, then `npm run dev`
- For production validation: `npm run build`
- Do not add Base44-specific tooling, config, or SDK integrations unless a specific requirement requires it
